[project]
name = "tradepy"
version = "0.1.0"
description = "Funciones reutilizables para mis proyectos y notebooks"
authors = [
    { name="Lijun Zhou" }
]
readme = "README.md"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
